# SG-ERM 后端依赖
# Python 3.11+

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # FastAPI 表单/文件上传

# 模板
jinja2>=3.1.3

# 数据库 (SQLAlchemy 2.0 异步 + SQLite)
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
alembic>=1.13.0

# 异步 HTTP 客户端 (同步引擎下载扩展包)
aiohttp>=3.9.1

# 配置管理
pydantic-settings>=2.1.0

# 安全
python-jose[cryptography]>=3.3.0  # JWT
passlib[bcrypt]>=1.7.4  # 密码哈希
cryptography>=42.0.0  # RSA 密钥对生成与签名

# 调度
apscheduler>=3.10.4

# 监控
prometheus-client>=0.19.0
